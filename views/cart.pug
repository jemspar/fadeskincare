extends layout

block scripts
    script(src="https://www.paypal.com/sdk/js?client-id=AcW6eH0xbAOeytXMN0dNv4wfY78uWAi5adeNLTVLnBno0RSNONH2FsSvc9RLLrAWDDH1V7WX_PaRLcIp")

block content
    h1 cart
    if cart
        each cartItem in cart.genArray()
            div(class="cart-item",id=cartItem.item._id)
                span=cartItem.item.name + ",  quantity " + cartItem.qty
        h3
            em
                = "Total: $" + cart.totalPrice
        #paypal-button-container
        script(src="/javascripts/paypal-test.js")
    else
        p Your cart is empty. Why not go 
            a(href="/")
                | add something?



    